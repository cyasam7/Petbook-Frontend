const reducer = (state, action) => {
    switch (action.type) {
        case "PRUEBA":
            console.log("prueba");
            
            break;
    
        default:
            break;
    }
};
export default reducer
